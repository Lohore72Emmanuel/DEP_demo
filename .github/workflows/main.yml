name: name

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Build step
        run: echo "Demo build"

  test:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Test phase
        run: echo "Running test cases"

  deploy:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Deploy app
        run: echo "Deploying the app"
