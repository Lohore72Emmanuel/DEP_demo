Name:Helle world
  on:workflow_dispatch
  Jobs:
    build:
    runs-on:ubuntu-latest
       steps:
        name:build Step
          run:echo A "demo build"
          test:
          runs-on:ubuntu-latest
              steps:
              name:test phase
                run:echo A "Running test cases"

                deploy:
                 runs-on:ubuntu-latest
                   steps:
                        name:deploy the app
                         run:echo A "Deploying the app"
                         
          
